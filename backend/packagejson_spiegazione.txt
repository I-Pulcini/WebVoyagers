"name": "backend"
"Ho impostato il nome 'backend' per indicare chiaramente che questa cartella contiene tutta la logica del server del mio progetto, separandola fisicamente dal frontend."

"version": "1.0.0"
"Ho lasciato la versione iniziale '1.0.0' perché questa rappresenta la prima release ufficiale e funzionante del mio progetto per l'esame."

"description": ""
"Ho scelto di lasciare questo campo vuoto perché il nome della cartella e il contesto del progetto universitario spiegano già a sufficienza lo scopo di questo server."

"main": "server.js"
"Ho specificato 'server.js' al posto del classico 'index.js' di default. L'ho fatto perché ho deciso di chiamare così il mio file principale, indicando chiaramente a Node quale file deve leggere per far partire tutto il motore del mio Back-end."

"scripts": { "test": "echo \"Error: no test specified\" && exit 1" }
"Qui ho mantenuto lo script di test predefinito. Per avviare il mio server non ho creato uno script personalizzato, preferisco avere il controllo manuale e digitare direttamente il comando node server.js nel terminale."

"keywords": [], / "author": "", / "license": "ISC"
"Ho lasciato questi tre campi vuoti o con i valori di default. Sono metadati che mi servirebbero solo se decidessi di pubblicare il mio codice online su npm come pacchetto open-source, ma per un progetto accademico privato non sono rilevanti."

"type": "commonjs"
"Ho mantenuto il tipo 'commonjs' perché nel mio Back-end ho scelto di importare le librerie usando la sintassi tradizionale di Node.js, ovvero tramite la funzione require(). Questo lo differenzia dal mio Front-end in Vue, dove invece utilizzo il sistema a moduli ES con la parola chiave import."

"dependencies": { ... }
"In questa sezione c'è il cuore del mio server. Qui ho elencato tutte le librerie esterne che ho installato tramite il terminale e che sono state scaricate nella cartella node_modules:"

"bcrypt": "^6.0.0"
"Ho installato bcrypt perché la sicurezza è fondamentale. Uso questa libreria per prendere la password in chiaro digitata dall'utente e trasformarla (attraverso un processo di hashing) in una stringa illeggibile prima ancora di salvarla nel database."

"connect-pg-simple": "^10.0.0"
"Ho scelto questa libreria per gestire la persistenza delle sessioni. La uso in combinazione con express-session per salvare fisicamente i dati dei cookie degli utenti loggati all'interno di una tabella dedicata su Supabase, evitando che vengano persi se il server dovesse riavviarsi."

"express": "^5.2.1"
"Ho installato Express perché è l'impalcatura essenziale del mio Back-end. L'ho usato per creare il server web in modo rapido e per gestire in modo pulito tutte le rotte API che fanno comunicare il Front-end con il database."

"express-session": "^1.19.0"
"Ho usato questa libreria per gestire lo stato di autenticazione. Quando un utente fa il login, express-session crea un ID univoco e gestisce il cookie che viene inviato al browser, permettendo all'utente di rimanere connesso mentre naviga nel sito."

"pg": "^8.20.0"
"Ho inserito il pacchetto 'pg' (node-postgres) perché il mio database ospitato su Supabase è un PostgreSQL. Questa libreria mi serve come 'traduttore' e ponte di collegamento: permette al mio codice JavaScript di connettersi al cloud e di eseguire le query SQL."
